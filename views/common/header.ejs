<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <style>
        .header {
            width: 100%;
            height: 100%;
        }

        .title {
            text-align: center;
            margin: auto;
            text-decoration: none;
            color: black;
        }

        .layout {
            width: 100%;
            height: 50px;
            display: flex;
            justify-content: flex-end;
        }

        .item {
            text-decoration: none;
            /* 밑줄제거 */
            font-size: 0.9rem;
            display: flex;
            list-style: none;
            margin-right: 1rem;
            color: black;
            display: inline-block;
            transition: .3s;
            -webkit-transform: scale(1);
            transform: scale(1);
        }

        .item:hover {
            -webkit-transform: scale(1.5);
            transform: scale(1.5);
        }
    </style>
</head>

<body>
    <div class="header">
        <div class="layout">
            <%if(sess.info){%>
                
                
                <p style="margin-right: 100px;"> 등급 :<%=sess.info.urate%></p>

                <p style="margin-right: 100px;"> 보유 포인트 :<%=sess.info.upoint%></p>

            <a style="margin-top: 14px; margin-right: 30px;"><%=sess.info.uname%>님</a>

            <%if(sess.info.master==0) {%>

                

            <a href="/mypage">
                <p class="item">마이페이지</p>
            </a>

            <a href="/basket">
                <p class="item">장바구니</p>
            </a>
            <% } else { %>
            <a href="/registCom">
                <p class="item">컴퓨터 등록</p>
            </a>
            <% } %>
            <form action="/logout" method="post">
                <button type="submit">로그아웃</button>
            </form>
            <% } else { %>
            <a href="/login">
                <p class="item">로그인</p>
            </a>

            <a href="/join">
                <p class="item">회원가입</p>
            </a>
            <% } %>
        </div>
        <div>
            <a class="title" href="/">
                <h1>ComShop</h1>
            </a>
        </div>
    </div>
</body>

</html>